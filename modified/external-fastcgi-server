# Start an external fastcgi server, 3 processes, listening on the specified socket path
script/shinycms_fastcgi.pl -d \
	-n 3 \
	-l /tmp/capitolareaseascouts.org.socket \
	-p /tmp/capitolareaseascouts.org.pid

